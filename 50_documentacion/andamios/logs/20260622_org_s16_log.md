# Log — P-ORG: reorganización del directorio (slep_idps, sesión 16)

> Higiene de estructura en `50_documentacion/` y la raíz del repo. NO toca el motor,
> el parquet ni los datos. Encargo: `50_documentacion/activa/encargos/encargo_…` (pasado
> por chat, no archivado). Fecha: 2026-06-22. **GATE pre-push: sin push.**

## 1. Resumen y veredicto

**ÉXITO.** 16 encargos movidos a `activa/encargos/`; 8 scripts sueltos archivados a
`_archivo/20260622/`; 12 referencias full-path reescritas a la ruta nueva; integridad
referencial 0 referencias rotas; parquet y motor intactos. DRY_RUN verde (12 == 12)
antes del modo real. Andamio (`99_reorganizar_estructura.R`) archivado.

## 2. Inventario de commits

| # | Commit | Tema |
|---|---|---|
| 1 | `f73a07e` | chore(org): mover encargos a activa/encargos/ + reescribir referencias (P-ORG s16) — 16 renames + 12 reescrituras |
| log | (este) | docs(log): log P-ORG s16 |

**Commits que el encargo previó pero quedaron VACÍOS** (declarados, sin git diff):
- **(2) archivar scripts de raíz:** los 8 scripts (`verificar_*.R`, `reorganizar_universo_idps.R`)
  estaban **gitignorados** (no trackeados); moverlos a `_archivo/` (también gitignorado) no
  produce cambio en git. Es un `mv` de filesystem puro.
- **(3) `git rm --cached` DS_Store + `.gitignore`:** **ningún `.DS_Store` estaba trackeado**
  (ya los ignora `.gitignore` L20); `git rm --cached` es moot. El `.gitignore` ya tiene
  `.DS_Store`, `_archivo/`, `*.bak`, `/verificar_*.R` → no se agregó nada.
- **(4) archivar `backlog_volcado_crudo.md`:** **N/A** — QUEDA (lo referencian 4 `.md` vivos).

## 3. Cambios sustantivos (causa / verificación)

### 3.1 Mover 16 encargos a `activa/encargos/`
- **Causa:** 16 `encargo_*.md` mezclados en la raíz de `activa/` (desorden).
- **Verificación:** `activa/encargos/` tiene **16** `.md`; `activa/` raíz sin encargos sueltos
  (0); git los registró como **renames** (historial preservado).

### 3.2 Archivar 8 scripts sueltos de la raíz
- **Causa:** `verificar_*.R` (7) + `reorganizar_universo_idps.R` sueltos en la raíz del repo.
- **Verificación:** raíz con **0** de esos scripts; `_archivo/20260622/` los contiene (8 + el andamio).

### 3.3 Reescribir 12 referencias full-path (`.md`)
- **Causa:** mantener válidas las referencias a los encargos tras el movimiento (integridad referencial).
- **Qué:** `50_documentacion/activa/encargo_X.md` → `50_documentacion/activa/encargos/encargo_X.md`,
  en 10 logs (cabecera `> Encargo:`) + `traspaso_cierre_v11.md` (L87) + `decisiones/20260622_decision_paleta_indicadores.md` (L4).
- **Verificación:** `grep` de la ruta vieja en logs/traspasos/activa → **0**; cada ruta nueva
  **existe en disco**. DRY_RUN y real ambos = 12 reescrituras (cuadra con el diagnóstico).

### 3.4 Andamio archivado
- `99_reorganizar_estructura.R` (adaptado de `herramientas_dev/plantillas/…PLANTILLA.R`) →
  `_archivo/20260622/` tras el modo real. `DRY_RUN` revertido a `TRUE` (default seguro).

## 4. Verificación de invariantes (🔒)

| 🔒 | Invariante | Resultado | Evidencia |
|---|---|---|---|
| Parquet intacto | md5 `4c764d8c…` | **PASA** | ni se leyó; md5 igual al cierre |
| 30_proc / 20_ins / 40_sal / docs/ sin tocar | — | **PASA** | `git status -- esas rutas` = vacío |
| andamios/diseno no se toca | — | **PASA** | excluido del escaneo (`/50_documentacion/andamios/diseno/`) |
| _archivo no se reescribe (no falsificar registro) | — | **PASA** | `/_archivo/` excluido; el comentario de procedencia de `reorganizar_universo_idps.R` (L4, → ruta vieja del encargo) se conservó tal cual (opción 1 del titular: archivo congelado preserva rutas históricas, POLITICA §1.6) |
| DRY_RUN antes del real | — | **PASA** | DRY verde (16/8/12, conteo cuadra) antes de `DRY_RUN<-FALSE` |
| Referencias válidas tras mover | — | **PASA** | 0 referencias a la ruta vieja; nuevas existen |

## 5. Diagnóstico FASE 1 (referencias) — tabla

**Reescritas (full-path, 12 ocurrencias / 10 encargos):** ajustes_motor_s14 (1), display_fidelity (1),
fix_fantasma (1), integracion_historico (1), motor_historico (1), organizacion (1), **paleta (2)**,
**verificacion_integracion_historico (2)**, verificacion_valores_historico (1), paleta_v2_s15 (1) —
en 10 logs + traspaso_v11 + decisiones/decision_paleta.

**`encargo_autonomo_claude_code_v1.md`:** es la plantilla del KB (NO está en `activa/`) → sus
referencias NO se reescriben (fuera de alcance).

**Detención (a) — referencia en archivo NO-`.md`:** `reorganizar_universo_idps.R:4` (`# Encargo: …organizacion.md`).
Reportada; el titular eligió **opción 1**: archivar el `.R` sin tocar su comentario (registro histórico en `_archivo/`).

## 6. Pendientes / `# REVISAR` / notas para el revisor

- **Menciones bare NO tocadas** (prosa sin ruta, decisión del titular): `traspaso_cierre_v14.md` L116
  (`\`encargo_claude_code_idps_paleta.md\``), L162 (`\`encargo_ajustes_motor_s14.md\``),
  `traspaso_cierre_v15.md` L108 (`\`encargo_paleta_v2_s15.md\``), `paleta_indicadores_log.md` L149,
  `encargo_paleta_v2_s15.md` L129/L130, `traspaso_cierre_v11.md` L97 (`.../…valores_historico.md`, elipsis).
  Son referencias **textuales no-ruta**, no paths rotos.
- **`# REVISAR` — CSV `_archivo/log_reorganizacion.csv`:** preexistía (de s10, esquema de 9 columnas);
  el script **appendeó** 36 filas con su esquema de 5 columnas → CSV con **esquemas mixtos**. Es scratch
  gitignorado; si quieres un log limpio, regenerar a un archivo propio. No bloquea.
- **`.gitignore` y `reorganizar_*.R`:** `reorganizar_universo_idps.R` estaba cubierto por
  `.git/info/exclude` (local, no versionado), no por `.gitignore`. Como se archivó a `_archivo/`
  (gitignorado), **no se agregó** regla nueva (sería para un archivo que ya no está en la raíz).
- **Discrepancias FASE 0 vs supuestos del encargo:** 16 encargos (no 19); `.DS_Store` no trackeados;
  scripts ya gitignorados. Todo adaptado al estado real.

## 7. Estado / gate

- `git status` post-migración: **limpio** (salvo este log antes de commitearlo).
- **GATE pre-push: NO se pushea.** Los commits quedan locales esperando tu confirmación.
- Parquet `4c764d8c9f0bf70004f8aa52661ae901` inicio == fin.
