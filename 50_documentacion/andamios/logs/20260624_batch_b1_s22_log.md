# Log de ejecución — Batch B / Encargo 1 (s22)

**Fecha:** 2026-06-24
**Encargo:** `50_documentacion/activa/encargos/encargo_claude_code_idps_batch_b1_s22.md`
**Modo:** autónomo, secuencial, 3 fases en orden (#23 → #13 → #18), este turno.
**Alcance:** edición de un único archivo `30_procesamiento/35_motor_template.html`.
Sin R, generador, parquet ni payload.

**Estado de cierre especial:** **NO commit, NO push, NO deploy.** Los 3 cambios quedan en
el working tree del template, **encima de los 4 commits de Batch A** (HEAD `3cf666c`) y del
build local regenerado. El titular decide el commit del lote Batch B después.

---

## 1. Resumen

Tres mejoras sobre el template, todas verificables en navegador:

1. **Fase 1 (#23):** token `--st-neutro` `#8C8A86` → `#7E8A99` (gris-azulado), separado del
   `--gris` de UI (`#6b7780`) y de los grises "off"; punto medio del eje rojo→neutro→azul.
   Comentario de los tokens de estado (L18) actualizado (documentaba el color viejo).
2. **Fase 2 (#13):** glifo de significancia **temporal** (vs evaluación anterior) junto a cada
   número del histórico (`BarrasAnio` / `.ybar-val`): dirección por signo de `dif`, color por
   `sig` (reusa clases `de/al/nt`). El primer año con dato no lleva glifo (`dif===null`).
3. **Fase 3 (#18):** tira externa (`.s100-ext`) bajo la barra apilada del comparador para los
   segmentos finos (`<9%`) que no caben dentro; cada uno con glifo de estado, color y % .

Build local regenerado (`run_all(only=35)`). **Payload byte-idéntico**, parquet intacto.

---

## 2. Por cambio: verificación

### Fase 1 — #23 (CSS, 1 token)
- **Zona:** `--st-neutro` (L24). Consumido por el segmento neutro de `StackedBar` (L754),
  los glifos `=` de `HistTrend`/marcas (`.nt`, L367) y la leyenda "= sin diferencia" (L1388/L1577).
- **Cambio:** `--st-neutro:#7E8A99;`. No se tocó `--gris`, `--alerta`, `--destaca`. Comentario
  de L18 actualizado (decía "neutro #8C8A86"; 0 refs restantes al color viejo).
- **Verificación (navegador):** `getComputedStyle(:root)['--st-neutro']` = `#7E8A99`.
  Contraste del % interno (🔒): `_txtOn('#7E8A99')` → luminancia
  `(0.299·126+0.587·138+0.114·153)/255 = 0.5338 < 0.55` → **`#fff`** (texto blanco legible
  sobre el segmento neutro). En el comparador el segmento neutro y los glifos `=` se ven
  gris-azulado, distinguibles del gris de texto. Sin error de consola.

### Fase 2 — #13 (markup + CSS)
- **Zona:** render de `.ybar-val` dentro de `BarrasAnio` (L944); CSS junto a `.ybar-val` (L344).
  Datos por punto desde `serieEje` (`{a,v,dif,sig,estado}`), no se tocó.
- **Cambio:** dentro de `<span className="ybar-val">`, tras el número, glifo condicional a
  `p.dif!=null`: dirección `p.dif>0?▲:(p.dif<0?▼:=)`, clase `(sig===±1)?(dif>0?de:al/nt):nt`,
  `title` "vs evaluación anterior: ±N pts (significativo|solo aritmético)". CSS: `.ybar-sig`
  `var(--fs-2xs)` negrita; `.de→--destaca`, `.al→--alerta`, `.nt→--st-neutro`. Se añadió
  `white-space:nowrap` a `.ybar-val` para no romper la línea número+glifo.
- **Verificación (RBD 1692, Escuela Arturo Prat Chacón, vista histórica):** 19 glifos en la
  ficha. Casos representativos (dirección por `dif`, color por `sig`):
  - **Significativo ▲** azul `--destaca` (rgb 42,143,217): "+5 pts (significativo)" — `dif>0 & sig` → `de`.
  - **Significativo ▼** rojo `--alerta` (rgb 238,45,73): "−7 pts (significativo)" — `dif<0 & sig` → `al`.
  - **Solo aritmético** gris-azulado `--st-neutro` (rgb 126,138,153): "+4 pts (solo aritmético)" → `nt`.
  - **Primera barra de la serie (2014) SIN glifo** (`dif===null`); columnas off (2019–2021)
    sin valor ni glifo. Tooltips coherentes. Sin error de consola.

### Fase 3 — #18 (markup + CSS)
- **Zona:** `StackedBar` (L750); etiqueta interna por umbral (≥16% "% (N)" / ≥9% "%" / <9% nada).
- **Cambio:** el return pasa de un único `<div className="s100">` a un fragmento que contiene la
  barra + una tira externa condicional `parts.filter(s=>s.p>0&&s.p<9)`; cada ítem
  `.s100-ext-it` con glifo `{bajo:▼,neutro:=,sobre:▲}`, `style={{color:s.c}}` (`s.c` incluye
  `var(--st-neutro)` de #23) y `title` completo. CSS: `.s100-ext` (flex, wrap, margin-top:3px,
  `var(--fs-2xs)`) y `.s100-ext-it` (negrita, nowrap). No se tocó `repartoInd`/`pctRound`.
- **Verificación (comparador, 8 SLEP):** 6 tiras externas renderizadas; ítems con glifo+color
  de estado, p.ej. **SLEP Chiloé (Clima): "▼ 6%" rojo** y **SLEP Andalién Costa (Hábitos):
  "▼ 6%" rojo** bajo la barra. `aria-label`/`title` del contenedor conservados.

**Gate de convergencia (#18, regla de detención c): PASA.**
- A ancho normal (pane 1150px): `tableScrollW==scrollClientW` → **sin desborde horizontal**;
  8 filas × 5 columnas intactas; la tira queda **debajo de la barra** (`strip.top>=bar.bottom`).
- **0 ítems de tira desbordan su celda** (`<td>`): ítem más ancho 28.8px, cabe holgado en las
  columnas de indicador → la tira agrega solo alto vertical, no ancho.
- Al ancho angosto del panel de preview (~418px) hay 18px de scroll horizontal en `.cmp-tscroll`,
  pero es la afordancia de scroll **intrínseca de la tabla** a ese ancho (el `.cmp-tscroll` ya la
  provee por diseño) y **no la causa #18** (las tiras caben en sus celdas). No es degradación real.

---

## 3. Verificación de invariantes 🔒

| Invariante | Resultado | Evidencia |
|-----------|-----------|-----------|
| Parquet `idps_largo.parquet` md5 `4c764d8c…` intocable; payload byte-idéntico | **PASA** | md5 pre/post = `4c764d8c9f0bf70004f8aa52661ae901`; blob base64 `atob("…")` regenerado == previo (`cmp` idéntico). |
| #13 y #18 NO cambian cálculo (`serieEje`/`repartoInd`/`pctRound` intactos) | **PASA** | Solo se añadió render a partir de campos ya devueltos (`dif`/`sig`/`parts`); esas funciones no se editaron. |
| Bug s7-1: ningún comentario CSS contiene `*/` salvo el cierre real | **PASA** | Comentarios nuevos/editados (token estado, `.ybar-sig`, `.s100-ext`) revisados; sin `*/` interno. |
| Significancia siempre LEÍDA del dato (`sigdif` por celda), nunca recalculada | **PASA** | El glifo usa `p.sig` (de `sigdif` vía `serieEje`); no se recalcula nada. |
| Glifo de #13 usa significancia TEMPORAL (`sig`/`sigdif`), no la de GSE (`sigdifgru`) | **PASA** | `serieEje` lee `sigdif`; los `title` dicen "vs evaluación anterior". `repartoInd` (que usa `sigdifgru`) no se tocó. |

---

## 4. Estado del working tree y cierre

- **HEAD:** `3cf666c` (último commit de Batch A). **Sin nuevos commits.**
- **Template** `30_procesamiento/35_motor_template.html`: **modificado, sin commitear**
  (5 hunks runtime de las 3 fases + 1 hunk de comentario de doc del token de estado).
- **Build local** `40_salidas/motor_idps.html`: regenerado y verificado (modificado, sin commitear).
- **`docs/index.html`:** NO tocado (deploy gateado).
- **NO push.** El titular decide el commit del lote Batch B.
- **Pendientes:** ninguno técnico abierto en este lote.
